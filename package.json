{
  "name": "app-monitor",
  "version": "1.0.0",
  "private": true,
  "description": "Track competitors' Google Play listings over time.",
  "scripts": {
    "install:all": "npm install --prefix apps/api && npm install --prefix apps/web && npm --prefix apps/api run install:browser",
    "api": "npm --prefix apps/api run dev",
    "worker": "npm --prefix apps/api run worker",
    "web": "npm --prefix apps/web run dev",
    "enqueue-daily": "npm --prefix apps/api run enqueue-daily"
  }
}
